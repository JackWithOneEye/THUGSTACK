{
  "name": "thug-stack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:all": "esbuild index.js --bundle --minify --outfile=dist/index.js && npx tailwindcss -i input.css -o dist/output.css",
    "build:js": "esbuild index.js --bundle --outfile=dist/index.js",
    "tailwind:watch": "npx tailwindcss -i input.css -o dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.18.11",
    "tailwindcss": "^3.3.2"
  },
  "dependencies": {
    "htmx.org": "^1.9.2"
  }
}
